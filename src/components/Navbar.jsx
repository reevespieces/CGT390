const Navbar = () => {
    return(
        <div className="topnav">
            <a href="#home">Home</a>
            <a href="#about">About</a>
            <a href="#profile">Profiles</a>
        </div>

    );
};

export default Navbar;